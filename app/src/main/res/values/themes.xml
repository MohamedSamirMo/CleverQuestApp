<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.QuizScoreApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/toolbarBack</item>

        <item name="android:statusBarColor">@color/toolbarBack</item>"
    </style>

    <style name="Theme.QuizScoreApp" parent="Base.Theme.QuizScoreApp" />
</resources>